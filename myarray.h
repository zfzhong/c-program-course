#ifndef MYARRAY_H
#define MYARRAY_H

/*
 * This is a list of the functions that
 * I develop to handle arrays.
 * 
 */
int sort_array(int *a, int len);

int print_array(int *a, int len);

int find_min_and_swap(int *a, int len);

#endif

